# 背景
* 背景图内容：背景色，图片，渐变色。
* 背景图位置。
* 背景图大小。
* 背景图重复。

## 背景图位置
* 水平方向: 
  * 数值。如: 30px。
  * 百分比。 如: 30%。
  * 位置字符串: left | center | right
  * 位置字符串 数值。 如: `right 10`，表示，距右边 10px。
* 垂直方向。和水平方向的值类似，主要不同的地方是位置字符串。
  * 位置字符串: top | center | bottom。